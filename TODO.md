# TODO: Deploy Laravel Project to Vercel

- [x] Rename `api/index.php` to `vercel.json` in root directory
- [x] Create new `api/index.php` that bootstraps Laravel for all requests
- [x] Update `vercel.json` to route all requests to `/api/index.php`
- [x] Advise user on setting up external database and environment variables in Vercel
- [x] Test deployment and handle potential issues with views/sessions in serverless
